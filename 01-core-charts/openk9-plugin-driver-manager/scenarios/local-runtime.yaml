# This scenario creates a single standalone PluginDriverManager
# machine, with the most basic configuration and limited
# resource to be fit in a local K8s/K3s/Minukube environment


# OpenK9 Plugin Driver Manager configuration
config:
  jvmOpts:
    heapMin: "256m"
    heapMax: "1g"
    permGen: "64m"
    permGenMax: "256m"



